import {SafeAreaView} from "react-native";
import {Text} from "native-base";

function Register() {
    return (
        <SafeAreaView>
            <Text>Register</Text>
        </SafeAreaView>
    );
}

export default Register;
